<%= form_for(@startup_resource) do |f| %>
  <% if @startup_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@startup_resource.errors.count, "error") %> prohibited this startup_resource from being saved:</h2>

      <ul>
      <% @startup_resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
