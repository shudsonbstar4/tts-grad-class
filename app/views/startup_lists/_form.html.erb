<%= form_for(@startup_list) do |f| %>
  <% if @startup_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@startup_list.errors.count, "error") %> prohibited this startup_list from being saved:</h2>

      <ul>
      <% @startup_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
